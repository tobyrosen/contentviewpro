name: CI
# Thin caller — CI logic lives in the reusable workflow at
# tobyrosen/oss-workflows, pinned @v1 (build/test/lint/secrets/dep-scan,
# auto-detected per language: JS/TS + Rust here).
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    uses: tobyrosen/oss-workflows/.github/workflows/ci.yml@v1
# CI verification run 2026-08-19 — exercises tobyrosen/oss-workflows@v1 with Semgrep blocking. This PR is closed unmerged.
