name: Release

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write
  issues: write
  actions: read
  id-token: write

jobs:
  release:
    uses: tobyrosen/oss-workflows/.github/workflows/release.yml@664ea1ccf88efd6dae49a1d5141b733496514dd1 # v1
    with:
      profile: standalone-software
      config-file: release-please-config.json
      manifest-file: .release-please-manifest.json
    secrets: inherit
